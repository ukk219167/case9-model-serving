# Development + CI dependencies — not installed in the production image.
-r requirements.txt

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0                      # ASGI test client for FastAPI

# Linting / formatting
ruff==0.4.9                        # linter + formatter (replaces flake8 + black)
mypy==1.10.0                       # static type checking

# Type stubs
types-requests==2.32.0.20240523
